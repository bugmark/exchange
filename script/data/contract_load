#!/usr/bin/env ruby

require "./config/environment"

# TODO: only loop over Asks before mature-date and expire-date
Ask.unassigned.each do |ask|
  RewardCmd::Cross.new(ask, contract_maturation: Time.now).save_event.project
end